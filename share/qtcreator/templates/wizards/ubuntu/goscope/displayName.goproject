/* File generated by Qt Creator (with Ubuntu Plugin), version 3.0.1 */

import GoProject 1.1

GoProject {
    Application {
        name: "%ProjectName%"

        GoFiles {
        }

        Files {
            directory: "."
            paths: ["manifest.json","%ClickHookName%.apparmor"]
        }
    }
    Package {
        name: "launchpad.net/go-unityscopes/v1"
        Files {
            filter: "*.*"
        }
    }
}
